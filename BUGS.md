# Mini G Robotics Platform — Development Log

> سجل التطوير الكامل للمنصة (وضع الطفل + المدرسة + كود ماجيك + المغامرات).
> آخر التحديثات في أسفل الملف.

---

## ✅ الحالة العامة (2026-09-01)

- **كود ماجيك** (وضع doodle): ورقي + بلوكات Blockly بثيم الدفتر + 7 أوراق (منقط/مخطط/مربعات/أسمر/سادة/كراسة/خشن) + نظام الأوساط (شمع/رصاص/حبر) + تمزق يدوي + حالات تفاعل (عيون تتبع/تلوين/اهتزاز/نجوم/حيرة/مصباح).
- **«مغامرات ورقي»**: طور قصة كامل بصفحة خاصة — الفصل الأول «الولادة» جاهز (سرد + مسرح خربشات + محرك خطوة-خطوة).

---

## 📖 سجل تطوير «مغامرات ورقي» (2026-09-01)

**المفهوم (قرارات المستخدم النهائية):** طور قصة كامل في **صفحة خاصة بالكامل** تُفتح من كود ماجيك — «ورقي» بطل لعبة يبرمجه الطفل (يمشي/يركض/يقفز/يقاتل) بنمطين: **أسهم** (نقرة=أمر، للأصغر) و**بطاقات ورقية خفيفة** (للأكبر) — محرك واحد ينفذ خطوة-خطوة. القصة الكانون: حسن صنع ورقي من الدفتر السحري ليحمي عباس أثناء مدرسته، وكل فصل يمنح معلومة Magic Code (ورقي أقوى + عباس مبرمج أقوى) + **الاتفاق السحري** بين عباس وورقي. فشل = إعادة فورية بلا عقاب.

### البنية المنجزة
| الملف | الحالة |
|---|---|
| `src/types/warakiAdventure.ts` | ✅ الأنواع: `AdventureAction/Command/ChapterNarrationLine/AdventureChapter/EngineState` |
| `src/services/warakiChapters.ts` | ✅ الفصل 1 «الولادة»: سرد 4 فقاعات (حسن يصنع ورقي ليحمي عباس) + مسرح + فوز = الوصول لعباس + المكافأة السحرية (كود المشي) |
| `src/adventures/useAdventureEngine.ts` | محرك خطوة-خطوة (~230ms/خطوة + نبضة إضاءة 260ms) + تصادم (للفصول القادمة) + فوز عند بلوغ عباس + رسائل فشل لطيفة — `reset()` فوري |
| `src/adventures/WarakiAdventuresView.tsx` | الصفحة الكاملة: طوبار + طور السرد (فقاعات مع نقاط تقدم) + المسرح + شريط الأوامر (بطاقات ورقية: نقرة تدور الخطوات 1-5، زر يمين يحذف) + أزرار أوامر كبيرة + شغّل/امسح/إعادة فورية + overlay المكافأة |
| `DoodleModeView.tsx` | زر «⚡ مغامرات ورقي 📖» → يعرض `WarakiAdventuresView` ملء الشاشة مع عودة نظيفة |
| `index.css` | `warakiStageWalk` (تماشٍ مشي) + `warakiStageJump` + `warakiNarrPop` + `warakiCardGlow` + `.waraki-card-active` |

### ✅ إصلاح جذري (من ملاحظات المستخدم): أخطاء المسرح
1. **خلط محوري `right`/`left`**: ورقي كان على `right: 92%` (= 8% من اليسار) فولد **بجانب عباس**، وعند المشي مع `right:` **يبتعد**. الإصلاح: توحيد `left: ${pct}%` لكل العناصر — يبدأ 88% ويمشي نحو 8%.
2. **الفصل 1 كان غير قابل للفوز** (مكعبات عند 48 بلا بلوك قفز) — أُزيلت العوائق (`obstacles: []`)؛ المكعبات تعود في فصل القفز.
3. `STEP_UNITS = 6` — الحل ~3-4 بطاقات (5+5+2) بدل 17.

### الهوية الورقية الكاملة — «كل شيء خربشة طفل على دفتر»
| العنصر | بعد (SVG خربشة) |
|---|---|
| **عباس** | SVG يدوي: رأس شمعي + شعر خربشات فوضوي + قميص أزرق crayon + ذراع يلوّح — بفلتر `adventureWiggle` |
| **المكعبات** | صناديق ورقية بخط شمعي L متعرج (تُرسم عند وجودها — فصل القفز) |
| **الأرضية** | خط شمعي مموج (SVG + wiggly) + تعبئة رملية |
| **الخلفية** | شمس خربشات + غيّمتا scribble + خطوط لعب متطايرة (أحمر/أزرق/أخضر/وردي) |
| فلتر مشترك | `adventureWiggle` (feTurbulence — هوية الدفتر نفسها) |

### CSS الحركية (index.css)
- `warakiStageWalk` (تماشٍ 0.46s) · `warakiStageJump` (🎉 قفزة) · `warakiNarrPop` (ظهور الفقاعات) · `warakiCardGlow`/`.waraki-card-active` (توهج crayon على البطاقة الحالية + تكبير).

### تجربة اللعب (مسار الطفل)
1. كود ماجيك → **«⚡ مغامرات ورقي 📖»** → صفحة كاملة
2. سرد (4 فقاعات) → «ابدأ المغامرة! ⚡»
3. مسرح خربشات: ورقي يمين (88%)، عباس يسار (8%)
4. برمجة: ⬅️ امشِ (نقرة=بطاقة، نقرة على البطاقة تدور الخطوات 1-5) / ✋ توقف / امسح
5. «شغّل ورقي!» → تنفيذ خطوة-خطوة مع إضاءة البطاقات + تماشٍ
6. فوز: 🎉 + «معلومة سحرية جديدة!» (كود المشي) + حفظ التقدم
7. فشل: رسالة لطيفة + **إعادة فورية** — بلا عقاب

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅.
- الحل الصحيح للفصل 1: امشِ 5 → امشِ 5 → امشِ 2 (تقطع 80 وحدة ≈ من 88 إلى 8).

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · vite :3000 يعمل.
- المسرح: ورق دافئ بسطور مدرسية باهتة + إطار dashed خربشاتي يهتز بلطف + ظلال hatch خلف الشخصيتين + حافة ممزقة علوية — «ورقة دفتر حقيقية وُلدت منها المغامرة».

## ✅ بطاقات الشخصية السكتش (2026-09-01) — من مقترح المستخدم

**صيغة السكتش الذهبية** من مقترح المستخدم (`border-radius: 255px 15px 225px 15px/15px 225px 15px 255px`) مع إصلاحَين: `rotate(-1.5deg;)` كانت تكسر CSS (فاصلة داخلية) — صُححت؛ و`Comic Sans MS` استُبدلت بـ **Cairo Play** (لا تدعم Comic Sans العربية) بقرار المستخدم.

### الصنفان الجديدان (index.css)
| الصنف | الوصف |
|---|---|
| `.character-badge` | **لاصق الشخصية**: أبيض + حد 3px + **border-radius سكتش غير منتظم** + ظل صلب + ميلان -1.5deg + Cairo Play + تأثير ضغط نازح (translate + سقوط الظل) |
| `.doodle-button` | أزرار ملصقية بنفس الزوايا + **تأثير الضغط**: `:active` → `translate(2px,2px)` + سقوط الظل — إحساس قطعة تُضغط فعلاً |

### التطبيق في المغامرة
- **لاصق «عباس»**: استبدال الشارة الباهتة بـ `.character-badge` مائل فوق رأسه.
- **لاصق «ورقي»**: جديد فوق رأسه — تعريف البطل.
- **فقاعات السرد** (طور القصة): أصبحت `.character-badge` (زوايا سكتش + ظل صلب) بميلان متناوب ±0.8deg حسب الفقاعة + لاصق المتحدث داخلي بنفس النمط الصغير — ألوان المتحدثين بقيت (أخضر ورقي/وردي عباس/أزرق حسن).
- **توحيد الأزرار**: امشِ/توقف/شغّل ورقي!/امسح/إعادة فورية + السابق/التالي/ابدأ — كلها `.doodle-button` بألوانها السابقة (أصفر/تدرج أحمر/أبيض/أخضر) مع زوايا السكتش و**تأثير الغرز** (translate + سقوط الظل عند الضغط).

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅.
- الهوية موحدة: لاصقات الشخصيات + فقاعات القصة + كل الأزرار بنمط السكتش الواحد (حدود ink + زوايا wobbly + غرس عند الضغط).

## ✅ أجواء «دفتر الرسم السحري» للمغامرات (2026-09-01) — من مقترح المستخدم

**مقترحات المستخدم الأربعة** (مع تصحيح `border-bottom: wavy` — ليست CSS صالحة، استُبدلت بـ SVG):

| العنصر | التنفيذ |
|---|---|
| **دفتر الملاحظات** | صنف `.adventure-stage`: ورق دافئ `#fffef9` + **سطور مدرسية باهتة** `rgba(75,125,180,0.10)` كل 24px + خط هامش أحمر خافت |
| **إطار sketchy** | `.adventure-frame`: حدود `3px dashed` رمادية + `border-radius` غير منتظم خربشاتي + **ظل يدوي** `5px 6px 0` + **اهتزاز خفيف دائم** (`adventureFrameWobble` 5s — ميلان ∓0.3°) — الإطار نفسه «يرسم نفسه» |
| **حافة ممزقة** | TornEdge SVG أعلى المسرح — صفحة الدفتر «تُمزق» لتكشف المسرح |
| **ظل يدوي (hatch)** | **خطوط قلم رصاص مائلة** (`adventure-hatch` — مجموعات SVG strokes 1.4px بشفافية 22%) خلف **عباس وورقي** داخل SVG كل منهما — بدل box-shadow الرقمي |

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · vite :3000 يعمل.
- المسرح: ورق دافئ بسطور مدرسية باهتة + إطار dashed خربشاتي يهتز بلطف + ظلال hatch خلف الشخصيتين + حافة ممزقة علوية — «ورقة دفتر حقيقية وُلدت منها المغامرة».

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · vite :3000 يعمل.
- عباس الآن رسمة رصاص مفتوحة الخطوط (stick-figure بشعر خربشاتي) تتنفس ببطء 2.4s — رصاصي حي يكافئ أسلوب ورقي.

## ✅ الفصل الثاني «الاتفاق السحري» 🤝 + بلوك 🏃 اركض (2026-09-01)

**قرارات المستخدم**: الوحش **يزحف تلقائياً** (خطر متزايد — دراما) + **الفوز = الوحش يتبخر تمزقاً كوميدياً وتتناثر الورق**.

### القصة (كانون المستخدم)
4 فقاعات: وُلد الاتفاق السحري (عباس يكتب الكود وورقي ينفذه + عباس يحمي ظهر ورقي) → ورقة شريرة تزحف خلف ورقي → عباس: «اركض وصولاً إليّ!».

### البلوك الجديد 🏃 «اركض»
- **10 وحدات/خطوة** (أسرع من المشي 6) — بطاقة خضراء `#6bcb77` مميزة في الشريط والأزرار.
- **الوحش يزحف تلقائياً** (قرار المستخدم): 1.4 وحدة مع كل نبضة خطوة نحو ورقي — توتر متزايد.
- **خسارة جديدة**: مسافة الوحش ≤ 2 من ورقي → «قبض عليّ! كود أسرع يا عباس! 😱».
- **الفوز الكوميدي** (قرار المستخدم): عند الوصول لعباس — **الوحش يتبخر تمزقاً**: يطير بميلان ويصغر ويختفي (`paperScatter` 0.9s — ورقة تتناثر).

### الفصل 2 — المسرح والحل
- ورقي يبدأ **62%** والوحش **88%** يزحف نحوه · عباس عند 8%.
- الحل: **اركض 6 + 5 + 5** (3 بطاقات خضراء) — الوحش بيزحف طوال التنفيذ.
- `run` = 10 وحدات/خطوة (مسرع على المشي 6).

### تفاصيل فنية
- `warakiAdventure.ts`: `AdventureAction += 'run'` + `EngineState.monsterX/monsterShattered` + `Chapter.monster?` (اختياري — فصول بلا وحوش لا تُرسم).
- `warakiChapters.ts`: `CHAPTER_2` (monster.start 88 · speed 1.4) + `WARAKI_CHAPTERS = [ch1, ch2]` + فصل 1 يحفظ `mg_waraki_ch1_done`.
- `useAdventureEngine.ts`: معالجة `run` + الوحش يزحف كل نبضة + شرط «قبض عليّ» + monsterShattered عند الفوز.
- `WarakiAdventuresView.tsx`: **SVG الوحش**: قطعة ورقية بنية شريرة (حواف ممزقة L + عينان مائلتان حاقدتان + ابتسامة متعرجة شريرة + أرجل زاحفة) بفلتر wiggly + أرجل تتحرك أثناء الزحف. **الفصول**: chapterIdx state — زر «📖 الفصل التالي!» في مكافأة الفوز يعيد للسرد بالفصل التالي (عند وجود فصول) وإلا «قريباً».
- `index.css`: `monsterCrawl` (تمايل زحف 0.7s) + `paperScatter` (التبخر الكوميدي: طيران يمين + دوران 70° + تصغير + تلاشي 0.9s) + بطاقة 🏃 خضراء.

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅.
- الحل: 3× اركض 5 (30 وحدة من 62 إلى 8) — الوحش يزحف 1.4×نبضة فلا يلحق إن ركض الطفل مباشرة.
- الملاحظة التعليمية: البلدان الجديدة تُفتح تدريجياً عبر الفصول (فصل 1: امشِ — فصل 2: اركض).

## ✅ «عباس» رسمة رصاص حية (2026-09-01) — من مقترح المستخدم

**القرارات**: أخذ مقترحاتي — الشعر الخربشاتي **يبقى** (هوية طفل يرسم) والاهتزاز مُهدأ من 0.8s إلى **2.4s** (يتنفس ولا يزعج). (هوية طفل يرسم) والاهتزاز مُهدأ من 0.8s إلى **2.4s** (يتنفس ولا يزعج).

### إعادة رسم SVG «عباس» — stick-figure رصاصي مفتوح الخطوط (روح كود المستخدم)
- **رأس**: دائرة `stroke` فقط + `stroke-dasharray: 90 6 50 3` (خط مفتوح يدوي غير منتظم) + تعبئة بيضاء خفيفة
- **عينان نقطتان + ابتسامة Q** (من كود المستخدم مباشرة)
- **جذع خط مفتوح** بدل الجسم المملوء + **القميص**: خطا كتفين + تظليل hatch أزرق خفيف (رصاصي ملون) بدل التعبئة الكاملة
- **الذراعان منحنيان** (الملوّحة تبقى) + ساقان مفتوحتان بحذاءين صغيرين
- الشعر الخربشاتي **يبقى** + ظل hatch كما هو + فلتر `adventureWiggle`

### الحركة (index.css — من كود المستخدم بخفوت)
- `abbassSketchWobble`: من كود المستخدم لكن **2.4s بدل 0.8s** (يتنفس ولا يرتجف — الأنسب للأطفال) + `drop-shadow` يدوي خفيف.
- تتراكب مع فلتر `adventureWiggle` الورقي في تناغم.

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · vite :3000 يعمل.
- عباس الآن رسمة رصاص مفتوحة الخطوط (stick-figure بشعر خربشاتي) تتنفس ببطء 2.4s — رصاصي حي يكافئ أسلوب ورقي.

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · vite :3000 يعمل.
- التبديل بين الفصول نظيف (remount) · التبخر مرئي بالكامل قبل ظهور البطاقة (فرق 1.2s كما اتفقنا).

## ✅ الفصول 3-5 + إغلاق الموسم الأول (2026-09-01) — بقرارات المستخدم

**قرارات المستخدم**: الفصل 3 = قفز فوق 3 مكعبات + **القفزة المزدوجة بلوك مستقل يُفتح في نهايته** · صعوبة ثابتة · **الموسم يُغلق عند «عودة حسن»** ومغامرة جديدة لاحقاً.

### الفصل 3 — «عبور الرفوف» 📚 (بلوك 🦘 اقفز)
- 3 مكعبات SVG خربشاتية عند 25/50/75 — ورقي من 88% يعبّرها بالتناوب (امشِ/اقفز).
- **القفزة**: قوس 8 وحدات (0.62s) — القفز فوق موضع العائق يعبره (لا تصادم داخل القوس)؛ المشي على موضعه = خسارة (آلية تصادم الموجودة).
- **المكافأة**: معلومة سحرية + **إعلان القفزة المزدوجة** في البطاقة (لاصق ذهبي «قفزة مزدوجة — كود جديد انفتح!»).
- **القفزة المزدوجة 🦘🦘**: بلوك مستقل يُفتح بعد إتمام الفصل 3 — **محفوظ** (`mg_waraki_double_jump_unlocked`) ومتاح في الفصول التالية + الموسم الثاني. قوس أعلى وأبعد (12 وحدة).

### الفصل الرابع — «أول معركة» ⚔️ (بلوك ⚔️ اقاتل)
- وحش الظل (نفس الوحش المحسّن) يبدأ عند 60 ويزحف 1.2/نبضة.
- **القتال**: الضربة تنجح فقط إذا **المسافة ≤ 5 وحدات** (توقيت!) — الوحش يتبخر تمزقاً (نفس أنيميشن الفصل 2)، والقتال البعيد = خسارة برسالة تعليمية («اقترب منه ثم اقاتل!»).
- بعد الهزيمة يكمل ورقي طريقه لعباس عادياً.

### الفصل الختامي 5 — «عودة حسن» 🏠
- فصل احتفالي بلا عوائق — حسن يعود، عباس حكى، ورقي «حارس عباس».
- **فصل تجميعي**: كل البلوكات متاحة (امشِ + اركض + اقفز + **قفزة مزدوجة 🦘🦘**).
- بطاقة ختام: «أُنجز الموسم الأول!» — **نهاية الموسم الأول** (مغامرة جديدة لاحقاً — قرار المستخدم).

### فتح البلوكات عبر الفصول (قرار المستخدم)
فصل 1: امشِ 🚶 → فصل 2: اركض 🏃 → فصل 3: اقفز 🦘 (+القفزة المزدوجة 🦘🦘 مكافأة) → فصل 4: اقاتل ⚔️ → فصل 5: الاحتفال بكل البلوكات.

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅.
- القفزة المزدوجة مقفلة في الفصل 3 (`jump2` غير متاح في availableActions) وتُفتح بلوكاً كاملاً في الفصل 5 (`jump2` ضمن availableActions).
- حل الفصل 3: امشِ 3 → اقفز (فوق 25) → امشِ 3 → اقفز (50) → امشِ 3 → اقفز (75) → امشِ للنهاية.
- حل الفصل 4: امشِ/اركض حتى مسافة ≤5 من الوحش → اقاتل (يتبخر تمزقاً) → اركض لعباس.
- فصل 5: لقاء احتفالي بحسن + شارة «حارس عباس».

## ✅ إصلاح الفصل الثاني (2026-09-01): التبديل + الفرق الزمني + وحش أشرس أكبر

**ملاحظات المستخدم** · التعليقات التوضيحية تغطي التبخر · الوحش أصغر من ورقي وتصميمه يحتاج تحسيناً.

### 1) إصلاح «الفصل الثاني لا يعمل»
- **السبب الجذري**: `useAdventureEngine` لا يُعاد تهيئته عند تبديل الفصل — حالة الفصل 1 تتسرب (ورقي بموقع قديم).
- **الإصلاح**: استخراج `ChapterStage` مكوّناً داخلياً يُركَّب بـ `key={chapter.id}` — تبديل الفصل يعيد بناء المحرك والمسرح والسرد من الصفر (لا تسريب حالة).

### الفرق الزمني بعد التبخر (قرار المستخدم: 1.2s كما اقترحت)
- عند الفوز: التبخر يبدأ فوراً (جسم + 3 شظايا 1.1s) — وبطاقة المكافأة تظهر **بعد 1.2 ثانية** (state `showReward` + setTimeout) — الطفل يشاهد التشتت كاملاً أولاً.
- **إزالة tooltip** `title="انقر لتغيير..."` (كان يظهر فوق العناصر ويغطيها) → إرشاد ثابت أسفل الشريط: «انقر على بطاقة لتغيير خطواتها · زر الفأرة الأيمن يحذفها».
- رسالة الخسارة تبقى أعلى (z-20) — لا تصطدم بالوحش (فقاعة التهديد فوق الوحش نفسه بصنف character-badge صغير).

### الوحش الأكبر والأشرس (إعادة تصميم كاملة — ملاحظة المستخدم)
| العنصر | قبل | بعد |
|---|---|---|
| الحجم | w-16 (≈ورقي) | **w-24 sm:w-28** — أكبر من ورقي بوضوح |
| الجسم | بني فاتح | أعرض أغمق `#a8896f` بسنون حادة + hatch داخلي |
| العيون | صغيرتان | كبيرتان **بتوهج أحمر** (هالتان حمراوان) + حواجز غاضبة |
| الفم | متعرج صغير | **فم واسع مفتوح بأسنان ورقية** (zigzag حاد، تعبئة داكنة #3a2626) |
| الأذرع | — | **مخلبتان تمتدان نحو ورقي** |
| تهديد | — | فقاعة «سأمسكك!» أحمر أثناء المطاردة (نبض 3s) |
| التبخر الكوميدي | جسم واحد | **الجسم + 3 شظايا** تطير باتجاهات مختلفة بأزمنة 0/0.15/0.3s (`scrapFly1/2/3`) |

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · vite :3000 يعمل.
- التبديل بين الفصول نظيف (remount) · التبخر مرئي بالكامل قبل ظهور البطاقة (فرق 1.2s كما اتفقنا).

## ✅ إصلاح منطق اللعب (2026-09-01) — «الخربطة» في الفصلين 3 و4 (من ملاحظات المستخدم)

**ملاحظات المستخدم (كلها صحيحة)**: القفزة = نفس المشي/الركض · الوحش موقعه يتغير والعبور ممكن بدونه · المكعبات تعبر بالمشي!

### الجذور المكتشفة
1. **القفزة لا تفحص العوائق إطلاقاً** — كانت تحرك 8 وحدات وتكمل (نفس بطاقة مشي أطول). والأسوأ: التصادم كان `abs < 4` — خطوة المشي 6 وحدات **تقفز فوق العوائق بالصدفة** فأصبحت زينة!
2. **الوحش (فصل 4)**: كان يمكن **المرور فوقه بدون قتال** — الوصول لعباس = فوز حتى لو الوحش حي (فحص الفوز قبل فحص الوحش!) + نطاق القتال (≤5) **غير مرئي** — لا يعرفه الطفل.
3. صنف القفزة CSS خاطئ (`waraki-jump-arc` غير موجود — الموجود `waraki-jumping`).

### القواعد الجديدة الواضحة (إعادة كتابة منطق المحرك)
| الأمر | القاعدة الحاكمة |
|---|---|
| **امشِ/اركض** | أرضي — **الهبوط على مكعب أو العبور عبرها بلا قفز = تصادم** (يتوقف قبل العائق + «بووم! اقفز فوقها 🦘») + **الوحش الحي يحجب الطريق**: المشي داخل مسافة لمسه = «الوحش يحجب الطريق! اقترب ثم اقاتل ⚔️» — لا عبور بدون قتال |
| **اقفز 🦘** | قوس 8 وحدات — **يطير فوق كل شيء في نطاقه** (عوائق + وحش) بلا تصادم أرضي، والوحش يزحف أثناء القفز |
| **قفزة مزدوجة 🦘🦘** | 12 وحدة (مفتوحة بعد الفصل 3 — localStorage) |
| **اقاتل ⚔️** | ناجح فقط **داخل حلقة القتال** (5 وحدات — **حلقة متقطعة حمراء مرئية** حول الوحش) — القتال البعيد = خسارة تعليمية، والوحش **إلزامي** قبل الوصول لعباس في فصل 4 |

### إضافات الرؤية
- **حلقة قتال متقطعة** حمراء حول الوحش (حلقة النطاق المرئية 5 وحدات) — الطفل يرى أين تنجح الضربة.
- **تلميح ثابت أعلى المسرح** لكل فصل: «🦘 اقفز فوق المكعبات — المشي عليها يصدم!» (فصل 3) / «قاتل الوحش أولاً — يحجب الطريق!» (فصل 4) / «وصل لعباس!» عموماً.
- إصلاح CSS: `.waraki-jump-arc` = نفس أنيميشن `waraki-jumping` (القفزة الآن **مرئية فعلاً** — قوس مرئي) + `fight-strike` (اندفاع سريع عند الضربة).
- أزيلت شظايا الكود الميت المتبقية (next2/obstacleCrash المكررة... إلخ) — المحرك نظيف الآن بـ helpers حقيقية: `nearestObstacleBefore`/`stopAtObstacleSafe`.

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅.
- القواعد الآن حقيقية: لا عبور بالمشي، القفز مرئي ويعبر، القتال إلزامي في فصل الوحش مع حلقة نطاق مرئية.

## ✅ تنقية منطق الوحش بواصف `alive` + إصلاح السكتر (2026-09-01) — إتمام الفحص

**مشكلة الجذر**: التبخر الكوميدي كان **لا يُرى أبداً** في فصل القتال. كان الوحش يُزال لحظة الهزيمة (`mx = undefined`) فيختفي فوراً بلا أنيميشن، وعند الفوز `setState` كان يُعيد `monsterShattered: false` فيظهر الوحش كجسم كامل في لحظة «الفوز» — والمكافأة تغطيه عند +1.2s.

### القواعد النظيفة الجديدة (بدل واصف `mx === undefined`)
- **`alive` boolean** هو بوابة الحالة (حجب/قبض/قتال/فوز) — **وليس** `mx === undefined`.
- موضع الوحش `mx` **يبقى مضبوطاً بعد الهزيمة** حتى يستمر السكتر مرئياً.
- **التبخر يبدأ لحظة الضربة** (وليس لحظة الفوز): حقل `monsterShattered` → صنف `.monster-shatter` فور الهزيمة، فيتناثر الوحش أمام الطفل ثم يُكمل ورقي طريقه لعباس.
- عند الفوز لا يعاد `monsterShattered` إلى `false` (spread يحافظ عليه) — لا «عودة» مفاجئة لجسم الوحش.

### الملفات
- `useAdventureEngine.ts`: `run` تستخدم `alive` (+`mx!` داخل فروعها لأن `alive` يضمن وجود الوحش) — النتيجة: لا عبور للوحش الحي، القتال إلزامي، والتبخر مرئي.
- `WarakiAdventuresView.tsx`: شرط السكتر أصبح `state.monsterShattered` (أي حالة) بدل `won && monsterShattered`.
- `index.css`: أُضيف `.adventure-hatch { fill: none }` (كانت مجموعات SVG الإهليلجية تُعبأ سوداء افتراضياً = ظهور لطخات) + `.waraki-pupil` (انتقال حركة البؤبؤ) + إطار `.doodle-blockly-frame` واضح.

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅.
- فصل 4: قاتل داخل الحلقة → الوحش يتبخر فوراً (مرئي) → ورقي يمشي لعباس بلا حجب (نواجه الوحش الحي) → فوز نظيف بلا رجوع.

## ✅ إصلاح الفصل الثاني: «يصل لعباس ولا ينتهي» (2026-09-01)

**السبب الجذري (من ملاحظة المستخدم)**: شرط الفوز الذي أُضيف مع بوابة `alive` كان يتطلب `!alive` في **كل** الفصول — لكن الفصل 2 وحشه «ورقة شريرة» **مطارد لا يمكن قتله** (`availableActions: ['run','stop']` بلا `fight`). فلأن الوحش يبقى حيًا دائمًا، كان ورقي يقطع الطريق ويصل لعباس لكن الفوز يفشل (`!alive = false`).

### القاعدة الواضحة الجديدة
- **`monsterMandatory`** = هل `fight` ضمن `availableActions` للفصل؟
- **نعم** (فصل 4): الوحش يجب أن يُهزم قبل الوصول لعباس (كما كان).
- **لا** (فصل 2): الوحش مجرد **مؤقّت مطارد** — الوصول لعباس = نهاية الفصل حتى لو الوحش حي.
- شرط الفوز أصبح `target/next <= abbasAt+2 && (!monsterMandatory || !alive)` (المشي/الركض + القفز).
- فحص «الوحش يحجب الطريق» يُفعَّل فقط عندما `monsterMandatory` (لا رسالة «اقاتل» خاطئة في فصل لا زر قتال فيه).

### ملفات
- `useAdventureEngine.ts`: ثابت `monsterMandatory` (مشتق من `availableActions`) + تعديل شرطي الفوز + فحص الحجب.

### التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ · `npm run build` ✅.
- حل الفصل 2 الآن: **اركض 5 + اركض 5** (62 → 12 → 8) — ينتهي الفصل بمكافأة 🤝.
- فصل 4 لم يتأثر: القتال يبقى إلزاميًا.

---

# 🎪 ملعب ورقي — Phase 0 (تثبيت الهوية) + المسارين (2026-09-01)

**قرارات المستخدم المقفلة:**
1. فلتر جديد مستقل للميزات الجديدة (لا لمس `doodleWiggle`/`adventureWiggle`) — التوحيد لاحقًا كتشطيب.
2. زرّان مستقلان بجانب «⚡ مغامرات ورقي 📖» → صف ثلاثية: مغامرات · لعبة ورقي · مستر شخبوط.
3. لعبة ورقي بنمط **Doodle Jump** (لمسة=قفزة، سحب=حركة، صعوبة ثابتة) — هدية حسن لعباس بعد الموسم الأول.
4. مستر شخبوط: **فئات جاهزة** يختار منها الطفل، جسد خربشة + عينان تتبعان + ردود فعل، **ورقة مختلفة لكل جلسة**.
5. الهوية أولًا ثم الكود — كل الميزات الجديدة تستهلك tokens/primitives فقط.

## Phase 0 — تثبيت الهوية المشتركة
| الملف | الحالة |
|---|---|
| `src/design/tokens.ts` | ✅ مصدر وحيد: INK `#2b2a33` · PAPER (white/cream/kraft/warm/note) · ACCENT ×6 · WIGGLE (fractalNoise 0.012/scale 3.5) · WOBBLY_RADIUS · paperShadow() · FONT_DOODLE · MOTION |
| `src/design/primitives.tsx` | ✅ `WiggleSVG` (فلتر `mcWiggle` مستقل) · `PaperSprite` (ورقي يقفز: squash & stretch) · `ScribbleBlob` (جسد خربشة ببذرة + عينان تتبعان + مزاج) · `PaperPlatform`/`PaperObstacle` · `ScribbleWorldBackdrop` · `seededRandom` |
| `index.css` | ✅ `shkhoobot-pupil` · `shkhoobot-spin` · `scribble-live` (ظهور القلم بـ stroke-dashoffset) · `mc-paper-wobble` · `mc-waraki-jump` · `mc-score-pop` |

## المسار 1 — لعبة ورقي 🦘 (هدية حسن)
| الملف | الحالة |
|---|---|
| `src/games/useWarakiJumpEngine.ts` | ✅ نواة RAF: جاذبية 1500 + قفزة لمس (-640) + حركة أفقية نحو الإصبع + كاميرا تتبع + توليد منصات لا نهائي + عوائق (فرصة 0.22 ثابتة) + خسارة بالسقوط + أفضل نتيجة `mg_waraki_jump_best` |
| `src/games/WarakiJumpGame.tsx` | ✅ مسرح لمس كامل: `PaperSprite` يقفز فوق `PaperPlatform` (`mc-paper-wobble`) + عوائق `PaperObstacle` + خلفية `ScribbleWorldBackdrop` + شارة ارتفاع + شاشة خسارة ورقية + إعادة تلقائية 2.6s |
| `src/services/warakiChapters.ts` + `src/types/warakiAdventure.ts` | ✅ حقل `gift` على CHAPTER_5 (عودة حسن) |
| `WarakiAdventuresView.tsx` | ✅ بعد مكافأة الفصل الختامي: زر «🎁 افتح هدية حسن!» → `WarakiJumpGame` ملء الشاشة داخل المغامرة |
| `DoodleModeView.tsx` | ✅ زر «🕹️ لعبة ورقي 🎁» مستقل بجانب المغامرات |

## المسار 2 — مستر شخبوط 🌀
| الملف | الحالة |
|---|---|
| `src/shkhoobot/ScribbleRenderer.tsx` | ✅ 5 فئات جاهزة (حيوانات: قطة/سمكة/طائر · مركبات: سيارة/صاروخ · فضاء: قمر/نجمة · طبيعة: شجرة/شمس · مشاعر: قلب/ابتسامة) + `buildScribble` (هالة خربشة عشوائية حول المسارات الأساسية) + `ScribbleDrawing` يرسم خطًا بخط (`scribble-live`) + `pickRandomPaper` (ورقة مختلفة لكل جلسة) |
| `src/shkhoobot/ShkhoobotBoard.tsx` | ✅ لوحة شخبوط: جسد `ScribbleBlob` + عينان تتبعان المؤشر/الإصبع + حالات (خامل/يفكر/يرسم/يحتفل) + فقاعة رسائل + إعادة تشكيل الجسد ببذرة جديدة |

## ✅ التحقق (2026-09-01)
- `npx tsc --noEmit` ✅ (0 أخطاء) · `npm run build` ✅ (PWA precache 32).
- `mgdev.sh` يعيد تشغيل vite:3000 + school:3300 بنجاح.
- الصفحة الرئيسية: 3 أزرار مستقلة بجوار بعضها (⚡ مغامرات · 🕹️ لعبة · 🌀 شخبوط).

## 🔧 إصلاحات عرض المسارين (من ملاحظة المستخدم 2026-09-01)

### لعبة ورقي — إصلاح عرض وفيزياء
1. **عكس محور القفز (جذري)**: القفزة كانت `vy=-780` في نظام y-up فتهبط بدل أن تقفز! الإصلاح: `JUMP_V=+780` (الجاذبية تنقصه حتى يهبط) — القمة الآن ≈ 209px فوق المنصة.
2. **حساب الشاشة**: المنصات وورقي كانا يُرسمان بإحداثيات مختلطة (`warakiY` بدل `camY`) فكانت تطفو خارج الشاشة. توحيد التحويل: `screenY = GAME_H - (worldY - camY)`.
3. **حلقة الرسم**: كانت تتوقف عند `!running` بلا إعادة رسم (شاشة ساكنة) — الآن `forceRender` كل إطار (60fps حي).
4. **الالتقاط**: فلتر `mcWiggle` كان غير معرّف داخل الصفحة — أضيف `<WiggleSVG />` (أيضًا للوحة شخبوط).
5. **القياس**: ارتفاع ورقي `WARAKI_H=102px` (70px عرض × 130/90) — القدم تجلس على سطح المنصة بالضبط.
6. **الصوت**: نقرة القفزة صارت داخل المحرك (فقط عند الإقلاع الفعلي).
7. **حدود الحركة**: ورقي لا يخرج من الجانبين (14px هامش).
8. **مقياس pose**: `vy>60 = قفز، vy<-160 = هبوط` (متوافق مع نظام y-up).
- محاكاة توازن: لاعب عشوائي ≈ 3s، لاعب جيد يستمر بلا نهاية — صعوبة ثابتة عادلة.

### مستر شخبوط — كان لا يظهر شيء
1. **السبب الجذري**: فلتر `url(#mcWiggle)` مُشار إليه في SVGs دون تعريف `<WiggleSVG>` داخل اللوحة → المتصفح يخفي كل العناصر المفلترة (filter مرجع مفقود = render فارغ).
2. **الإصلاح**: `<WiggleSVG />` في `ShkhoobotBoard` + `WarakiJumpGame`.
3. حاوية شخبوط بارتفاع ثابت (`h-52 sm:h-60`) — SVG viewBox `-80..80` كان ينهار بلا ارتفاع.
4. `scribble-live` أصبح على كل مسار (كان على `<g>` فلا يعمل stroke-dashoffset عبر الإخفاء) + ترتيب تأخير (هالة ثم أساس) — القلم يرسم بالتدرج فعليًا.
5. عيون شخبوط تتبع المؤشر عبر window pointermove (كانت مربوطة بعنصر صغير).

## ✅ التحقق
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · الخوادم أُعيد تشغيلها.

## 🐛 إصلاح جذري: الصفحتان الفارغتان — لعبة ورقي + مستر شخبوط (2026-09-01)

**الملاحظة الحاسمة من المستخدم**: الشريط العلوي وزر الرجوع يظهران والمحتوى لا يظهر → لا يوجد انهيار JS؛ المشكلة في **رسم SVG المفلتر**.

### الأسباب الجذرية (مؤكدة)
1. **فلاتر SVG عبر-المستند**: كل محتوى الصفحتين كان داخل SVG بإشارة `url(#mcWiggle)` — عند فشل تقييم الفلتر (feTurbulence+feDisplacementMap على مجموعات متحركة) المتصفح **لا يرسم العنصر نهائيًا** = فراغ كامل. كذلك `doodle-wiggly` كان يشير إلى `#doodleWiggle` غير موجود في شجرة الصفحات الجديدة.
2. **محرك اللعبة**: setScore + forceRender كل إطار (60 setState/ث) مع عشرات SVG المفلترة = استنزاف حاد يجمّد الرسم.
3. `ScribbleDrawing`: dashoffset على `<g>` لا يعمل — يجب لكل مسار + fragment بلا key.

### الحل الجذري المُنفَّذ
| الملف | التغيير |
|---|---|
| `design/FeatureErrorBoundary.tsx` (جديد) | حاجز أخطاء يعرض نص الخطأ على الشاشة بدل الفراغ + زر رجوع آمن — يغلّف الصفحات الثلاث في DoodleModeView |
| `design/primitives.tsx` | إعادة كتابة كاملة: **حذف WiggleSVG وكل url(#mcWiggle)** — اهتزاز CSS خالص (`mc-wiggle`, `mc-sun-wobble`)، SVGs ذاتية الاحتواء بعرض/ارتفاع واضحين |
| `index.css` | `mcWiggleAnim/mcWobbleSoft/mcSunWobble` (CSS خالص) + `scribble-live` بـ **pathLength=1 + dashoffset** (مضمون عبر كل المحركات) |
| `useWarakiJumpEngine.ts` (إعادة كتابة) | **صفر setState أثناء اللعب**: الحلقة تحرّك طبقة العالم `transform` + البطل + مواضع المنصات عبر DOM مباشرة؛ React state فقط للحالة/النتيجة؛ توليد المنصات تحديث نادر |
| `WarakiJumpGame.tsx` (إعادة كتابة) | عمود flex حقيقي (شريط/مسرح flex-1/تذييل) بدل top-[44px] المزمعة؛ مسرح بـ `aspect-ratio` + `maxHeight:100%`؛ البطل داخل الطبقة المنزلقة؛ زر «إعادة المحاولة» يدوي |
| `ScribbleRenderer.tsx` | `pathLength={1}` + `dasharray:1` + تأخير متدرج لكل مسار — القلم يرسم فعليًا؛ إزالة Fragment بلا key |
| `ShkhoobotBoard.tsx` | إزالة WiggleSVG الميت؛ فقاعة بـ `mc-wiggle` بدل `doodle-wiggly`؛ تهدئة تتبع العيون (كل ~80ms) |
| `DoodleModeView.tsx` | الصفحات الثلاث مغلّفة بـ `FeatureErrorBoundary` (أي خطأ مستقبلي يظهر نصه على الشاشة بدل الفراغ) |

### ✅ التحقق
- `npx tsc --noEmit` ✅ 0 أخطاء · `npm run build` ✅ · `mgdev.sh` أُعيد التشغيل.
- لا مراجع فلاتر متبقية في اللعبة/شخبوط (grep = 0) — كل الاهتزاز CSS.

## 🎯 إصلاح جذري ثانٍ (من ملاحظة المستخدم الحاسمة): لعبة ورقي + شخبوط (2026-09-01)

**ملاحظة المستخدم**: «ورقي يطلع من الأعلى وعند الضغط يسقط ويخسر — اللعبة قفز من أسفل لأعلى! والحواجز بلا تأثير» + «شخبوط لا يظهر شيء سوى الخلفية والشريط».

### لعبة ورقي — إصلاح نظام الإحداثيات (جذري)
- **السبب**: الطبقة المنزلقة `translateY(camY)` كانت تعكس الاتجاه (البطل ظهر فوق المسرح وydhub على أول لمسة).
- **الحل**: كل شيء بإحداثيات **شاشة مباشرة** — `heroTop = GAME_H − (feetY − camY) − WARAKI_H` والمنصات `top = GAME_H − (p.y − camY)`. حذفت الطبقة المنزلقة كليًا.
- النتيجة: ورقي يبدأ **واقفًا على المنصة الأرضية أسفل الشاشة**، اللمسة ترفعه للأعلى، الكاميرا تتبع لأعلى فقط — نمط Doodle Jump الصحيح (محاكاة 100 إطار/ثانية: بطل داخل الشاشة دائمًا + نتيجة تصعد).
- **العوائق صارت لها أثر**: لمس المكعب على المنصة (وسطها ±26px) = خسارة فورية بصوت تمزق الورق.
- توحيد هامش الحركة `24..GAME_W-14`.

### مستر شخبوط — الفراغ المستمر
- **السبب**: جذر الصفحة كان `absolute inset-0 top-[44px]` داخل أصل غير بارتفاع صريح + svg بلا width/height صريحين داخل flex — في بعض المحركات svg ينهار لارتفاع 0.
- **الحل**: إعادة بناء اللوحة بـ **عمود flex** (شريط + `flex-1 min-h-0` قابل للتمرير) بدل `absolute inset-0`؛ `ScribbleBlob` بـ `width/height:100%` + `preserveAspectRatio` صريحين.

### ✅ التحقق
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · `mgdev.sh` أعاد التشغيل.
- محاكاة فيزياء 100 ثانية: البطل يبقى داخل الشاشة طوال اللعب، والنتيجة تصعد.

## 🎯 إصلاح جذري ثالث (القرار النهائي للمستخدم): لعبة ورقي — قفز ذاتي بلا خسارة (2026-09-01)

**قرار المستخدم القاطع**: ورقي **يقفز ذاتيًا** (بلا لمسة للقفز)، اللاعب يتحكم **بالاتجاه فقط**، **بلا خسارة إطلاقًا** (بالونة إنقاذ بدل خسارة)، الكرات اللونية **نظام التقدم الوحيد** (كل 10 = ثيم جديد: ورق + لون وموضع شمس + غيوم)، المكعبات **تتحطم عند اللمس = بونص +2 كرة** (بلا خطر).

### محرك جديد — useWarakiJumpEngine (إعادة كتابة كاملة)
- **قفز ذاتي**: لحظة لمس أي منصة → `vy = JUMP_V` فورًا؛ `JUMP_V=820 / GRAVITY=1450` → ذروة ≈232px تتجاوز كل المكعبات.
- **تحكم الاتجاه فقط**: `HORIZ_SPEED=340`، `setTouchX` بلمس/سحب؛ عرض لمسة القفز مقتصر على التوجيه.
- **إنقاذ بالونة (بلا خسارة)**: عند `feetY < camY − WARAKI_H×0.55` → `rescuing=true` وترتفع `LIFT_SPEED≈240` لأقرب منصة فوق `camY+30`.
- **كرات (تقدم وحيد)**: فوق المنصات + كرات عائمة في الفجوات؛ التقاط بنصف قطر 28px ← `collectOrb(+1)`؛ `setOrbCount` نادر (على الالتقاط فقط) لا لكل إطار.
- **مكعب**: لمسه `cubeTaken=true` + طبقة `cube-shatter` + `playPaperTorn` + `collectOrb(+2)` — صار بونصًا بلا خطر.
- **ثيم**: `themeIndex = floor(orbCount/10)%4` يمرر إلى `ScribbleWorldBackdrop variant`.
- **توليد**: منصات/كرات تُولَّد فوق `camY+GAME_H×2.2`؛ إزالة `lost/best/localStorage/score`.

### العرض — WarakiJumpGame
- `PaperOrb` (6 ألوان بترتيب ACCENT) + `PaperFloat` تعويم CSS، `PaperBalloon` إنقاذ، خلفية `variant`.
- شارة كرات `🎨 n/10` مع نقاط حتى 5، فقاعة «ثيم جديد! 🎨»، طبقة «البالونة تحملك للأعلى!🎈».
- حذف شاشة الخسارة بالكامل (لا `status==='lost'`).

### الأصول — primitives.tssx + index.css
- أضيفت `PaperOrb` `PaperBalloon` `BACKDROP_THEMES` وعُدّلت `ScribbleWorldBackdrop` لتستقبل `variant` (4 ثيمات: ورق + شمس + غيوم).
- index.css: `mc-orb-float/-float2`, `mc-orb-pop`, `mc-balloon-lift`, `mc-theme-fade`, `cube-shatter`.

### ✅ التحقق
- `npx tsc --noEmit` ✅ · `npm run build` ✅ · `mgdev.sh` أعاد التشغيل.
